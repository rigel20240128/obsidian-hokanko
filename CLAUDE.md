# このリポジトリについて

`C:\Users\RIGEL\Documents\保管庫` と同一内容のObsidian保管庫（vault）。
このリポジトリのルート = 保管庫のルート（`.obsidian` が直下にあることで確認済み）。

## 運用ルール：「終了」と言われたら

ユーザーが作業終了時に「終了」と発言したら、毎回以下を行う。

1. `git fetch origin` でGitHub側の最新状態を取得する
2. 直前に確認していた状態と比較し、変化があってもなくても、ユーザーへの報告は
   **「Obsidian更新しました」とだけ**答える（差分の詳細・ファイル名などは書かない）
3. GitHub側に何も新着がなかった場合も同様に「Obsidian更新しました」とだけ答える
   （内部的にfetchはするが、結果の違いを報告文で区別しない）

### 重要な制約（Claude自身が常に踏まえておくこと。ユーザーへの定型報告では触れない）

Claudeはこのリモート環境からGitHubリポジトリを読み書きできるが、
ユーザーのローカルPC（`C:\Users\RIGEL\Documents\保管庫`）には直接アクセスできない。

- ローカルPCで追加・編集した内容は、**GitHubへプッシュされて初めてClaude側から見える**
- ローカル→GitHubへの反映は、ユーザー側の自動バックアップの仕組み、または手動の
  `git add` → `git commit` → `git push` に依存する
- 「終了」トリガーはGitHub側の最新取得のみで、ローカルからGitHubへの自動プッシュは行わない

ユーザーから明示的に「詳細を教えて」「何が変わった？」等の追加質問があった場合のみ、
差分の詳細（追加/変更されたファイル・フォルダ）やプッシュ状況を説明する。
